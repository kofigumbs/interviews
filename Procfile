# HEROKU CONFIGURATION
#
#   By default Heroku, installs dependencies from the Gemfile,
#   so we only need to start the server on the assigned port.
#
web: bundle exec rackup server/config.ru --port $PORT
